#include "Graph.h"
#ifndef RENDERER_H
#define RENDERER_H

class Renderer {
public:
    void renderGraph(Graph graph);

private:
    // Rendering data structures (e.g. graphics library)
    // ...
};

#endif // RENDERER_H
